/**
 * 用户状态
 */

type State = {
  // nickName: string;
};

const state = () => {};

const mutations: any = {};

const actions: any = {};

export default {
  namespaced: true,
  state,
  mutations,
  actions,
};
